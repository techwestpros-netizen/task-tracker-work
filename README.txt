Package Scan App v2

What's new:
- BarcodeDetector + ZXing barcode attempts
- OCR fallback with Tesseract.js
- FedEx-style tracking extraction heuristic
- Manual correction box
- Saved history includes source (Barcode / OCR / Manual)

Important:
- Best on mobile over HTTPS
- OCR is slower than barcode scanning
- This still cannot guarantee every label, but it is much stronger than the barcode-only version
